name = "Bag Spawn"
description = "Same settings as the default theme but spawning isn't fully randomized. A bag of the whole set twice is generated and taken from until empty, which then gets regenerated."
